def bin_search_OX(ss,x):
    mid = len(ss) // 2
    return ss != [] and \
           (x == ss[mid] or \
                                                      or \
                                           )