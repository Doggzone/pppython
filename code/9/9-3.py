def count_score(cards):
    score = 0
    number_of_ace = 0
    for card in cards:
        rank = card[1]
        # accumulate score (counts A as 11)






    # adjust score if score is over 21 and there is A
    # there may be two or more A's
    
    return score